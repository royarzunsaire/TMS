@model IEnumerable<SGC.Models.InventarioCaracteristicas>

@{
    ViewBag.Title = "Administrar Inventario Caracteristicas";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h3 class="page-title">Administrar Inventario Caracteristicas</h3>
<div class="portlet box blue-dark">
    <div class="portlet-title">
        <div class="caption">
            Inventario Caracteristicas
        </div>
        <div class="tools">
            <a href="javascript:;" class="collapse"> </a>
            <a href="javascript:;" class="fullscreen"> </a>
        </div>
        <div class="actions">
            <div class="inputs pull-right">
                <a href="@Url.Action("Create") " class="btn btn-circle btn-sm btn-default btn-group-lg" style="margin-right:10px; margin-left:10px;">
                    <i class="fa fa-plus-circle"></i> Nuevo
                </a>
            </div>
        </div>
    </div>

    <div class="portlet-body">
        <table class="table table-striped table-bordered table-hover dt-responsive dataTable" id="sys-table" cellspacing="0">
            <thead>
                <tr>
                    <th>
                        ID
                    </th>
                    <th>
                        Categoria
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Caracteristica1)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Caracteristica2)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Caracteristica3)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Caracteristica4)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Caracteristica5)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.idInventarioCaracteristica)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.categoria.Nombre)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Caracteristica1)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Caracteristica2)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Caracteristica3)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Caracteristica4)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Caracteristica5)
                    </td>
                    <td class="text-center">
                        @using (Html.BeginForm("Delete", "InventarioCaracteristicas", FormMethod.Post, new { @id = "notificationForm_" + @item.idInventarioCaracteristica }))
                        {
                            <div class="input-group-btn">
                                @Html.ActionLink(" ", "Edit", new { id = item.idInventarioCaracteristica }, htmlAttributes: new { @class = "btn btn-warning btn-sm glyphicon glyphicon-edit", @datatoggle = "tooltip", @title = "Editar" })
                                @Html.ActionLink(" ", "Details", new { id = item.idInventarioCaracteristica }, htmlAttributes: new { @class = "btn btn-info btn-sm glyphicon glyphicon-list", @datatoggle = "tooltip", @title = "Detalles" })
                                @Html.AntiForgeryToken()
                                <a class="btn btn-danger btn-sm glyphicon glyphicon-trash" href="javascript:confirmDelete('Delete','@item.idInventarioCaracteristica','InventarioCaracteristicas')" title="Eliminar"></a>
                            </div>
                        }
                    </td>
                </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section topLevel{
    @{Html.RenderPartial("_RenderConfirm.Modal"); }
}

