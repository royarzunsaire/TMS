@model SGC.Models.Relator

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Eliminar</h2>

<h3>¿Esta seguro que desea eliminar al relator?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.contacto.nombreCompleto)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.contacto.nombreCompleto)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.contacto.run)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.contacto.run)
        </dd>
    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

    <div class="form-actions no-color">
        @Html.ActionLink("Volver", "Index", new { }, new { @class = "btn btn-default" })
        <input type="submit" value="Eliminar" class="btn btn-default" />
    </div>
    }
</div>
