
@{
    ViewBag.Title = "Ingresar Asistencia";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="page-title uppercase">Bienvenido</div>

@*<div class="page-title" style="margin-bottom: 0">
    <div class="row">
        <div class="col-md-12">
            Bienvenido al Curso
        </div>
        <div class="col-md-4">
            <small>@ViewBag.curso</small>
        </div>
    </div>
</div>*@
@using (Html.BeginForm("IngresarAsistencia", "Participante", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()

    <div class="row">
        <div class="col-md-12">
            <div class="portlet light bordered">
                <div class="portlet-body">
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                    <input type="hidden" name="idComercializacion" value="@ViewBag.idComercializacion" />
                    <div class="row">
                        <div class="col-md-12 text-center">
                            <button type="submit" class="btn btn-primary btn-lg">Ingresar Asistencia</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
}