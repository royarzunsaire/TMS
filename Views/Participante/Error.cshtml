@model string

@{
    ViewBag.Title = "Error";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@Model</h2>

@Html.ActionLink("Volver", (string)ViewBag.returnAction, (string)ViewBag.returnController, new { }, new { @class = "btn btn-default" })

